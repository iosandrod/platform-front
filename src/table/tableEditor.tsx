import { defineComponent } from "vue";
import { } from '@visactor/vtable'
//核心表格组件
export default defineComponent({
    name: "TableEditor",
    setup(props, { slots, attrs, emit }) {
        return () => <div>tableEditor</div>;
    },
});